env:
  - REACT_APP_BACKEND_API_URL=http://$BACKEND_SERVICE_HOST:$BACKEND_SERVICE_PORT

hooks:
  post-checkout:
    cmd: npm install
  build:
    cmd: npm run build

daemons:
  app:
    run:
      cmd: npm start
      env:
        - HOST=0.0.0.0
        - PORT=3000
